#include <bits/stdc++.h>

using namespace std;

#define ll long long int


int main(){
  ll n;
  scanf("%lld", &n);

  ll sum = (n*(n+1))/2;

  if(sum%2!=0){
    cout << "NO";
  }
  else{
    sum = sum/2;
    for(int i=1)
  }
}
